// Section.types.ts

export interface SectionProps {
  children: React.ReactNode;
  background?: string;
  disabled?: boolean;
  visibility?: boolean; 
  className?: string;
  
  // Add visibility prop
}
